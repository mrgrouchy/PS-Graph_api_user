# Auto-detect text files and normalize line endings to LF in the repo
* text=auto

# PowerShell scripts — CRLF on checkout (Windows requirement)
*.ps1  text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf

# Markdown and config — LF everywhere
*.md   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Binary — no line-ending conversion
*.png  binary
*.jpg  binary
*.ico  binary
